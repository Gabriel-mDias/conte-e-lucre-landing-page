{
  "name": "sample-landing-page",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "verify": "node scripts/verify_page.mjs",
    "preview": "vite preview --open",
    "serve": "npm run build && vite preview --open",
    "process-media": "python scripts/process_media.py"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "lenis": "^1.1.20",
    "swiper": "^12.1.2"
  },
  "devDependencies": {
    "playwright-core": "^1.55.0",
    "vite": "^6.2.0"
  }
}
